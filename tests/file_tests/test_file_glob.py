import unittest


# TODO: make tests for this.
# The tests should first init the test files for recursive search like this:
# 1. Create directory, create a directory inside of that directory.
# 2. Add a file into both directories.
# 3. Clean them after (delete).
class TestFileGlob(unittest.TestCase):
    def setUp(self) -> None:
        print('TODO')


if __name__ == '__main__':
    unittest.main()
